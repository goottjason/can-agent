SERVICE="${SERVICE:-can-agent}"
CONTAINER="${CONTAINER:-projects-can-agent-1}"
IMAGE="${IMAGE:-can-agent-can-agent}"
ROUTE_PATH="${ROUTE_PATH:-/can-agent}"
ROUTE_OK_CODES="${ROUTE_OK_CODES:-200 301 302}"
REGISTRY_IMAGE="${REGISTRY_IMAGE:-ghcr.io/goottjason/can-agent}"
