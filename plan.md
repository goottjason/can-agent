# CAN-Agent 실시간 매매 모니터링 시스템 구축 계획

## 1. 개요

현재 시스템은 오전 9시에 1회만 매매 검사를 수행하여, 장중 가격 변동에 따른 CANSLIM/컵앤핸들 매수 신호를 포착하지 못합니다. 장중 5분마다 실시간 모니터링을 통해 매수 기회를 포착하고 즉시 주문을 실행하는 시스템으로 전환합니다.

## 2. 현재 시스템 분석

### 2.1 현재 동작
| 시간 | 동작 | 문제점 |
|------|------|--------|
| 오전 9시 | 자동매매 1회 실행 | DB의 어제 종가로만 분석 |
| 장중 (9:00-15:30) | 없음 | 실시간 가격 수집/분석 없음 |
| 오후 3시 30분 | 주가 동기화 | 장 마감 후라 매매 기회 상실 |

### 2.2 이미 구현된 컴포넌트
- `KoreaInvestmentApiClient.getCurrentPrice(stockCode)` - 현재가 조회 API (FHKST01010100)
- `CanSlimAnalysisService.analyze(stock)` - CANSLIM 6요소 분석
- `CupAndHandleAnalyzer.analyze(stock)` - 컵앤핸들 패턴 분석
- `TradingStrategyService.evaluateBuy/Sell()` - 매수/매도 판단
- `TradingStrategyService.executeBuy/Sell()` - 주문 실행

## 3. 포지션 사이징: 점수 기반 비례 배분

### 3.1 공식
```
총점 = CANSLIM 점수 + 컵앤핸들 점수 (각 종목)
종목별 배분 비율 = 해당 종목 점수 / 매수 대상 종목 총점
종목별 투자금 = 총 예수금 × 종목별 배분 비율
```

### 3.2 예시
- 예수금: 100,000원, 최대 보유종목: 10개
- 매수 신호: 종목A (점수 80), 종목B (점수 60)
- 총점: 140
- 종목A 배분: 100,000 × (80/140) = 57,142원
- 종목B 배분: 100,000 × (60/140) = 42,857원

### 3.3 제약 조건
- 한국투자증권 소수점 매매 최소 금액: 1,000원
- 최소 주문금액 제한 없음 (테스트 환경: 예수금 10만원)
- 1주 매수 불가능 시 해당 종목 스킵

## 4. 아키텍처 변경

### 4.1 신규 컴포넌트: IntradayMonitorWorker
```
장중 모니터 워커 (5분마다 실행)
├── 1단계: 현재가 조회 (한국투자증권 API)
│   └── 2,763종목 전체 현재가 수집
├── 2단계: CANSLIM/컵앤핸들 분석
│   └── 각 종목별 실시간 점수 계산
├── 3단계: 매수 신호 포착
│   └── 점수 기준 필터링 (기준 점수 이상)
├── 4단계: 포지션 사이징
│   └── 점수 기반 비례 배분 계산
└── 5단계: 주문 실행
    └── 한국투자증권 API 매수 주문
```

### 4.2 기존 컴포넌트 변경
| 컴포넌트 | 변경 내용 |
|----------|-----------|
| `AutoTradingWorker` | 장중 반복 모니터링으로 전환 (1회 → 5분마다) |
| `TradingStrategyService` | 점수 기반 배분 로직 추가 |
| `DataSyncScheduler` | 장중 현재가 저장 스케줄 추가 |

### 4.3 설정 추가
```yaml
trading:
  position-rate: 10           # 종목당 최대 배분 비율 (%)
  min-score: 60               # 매수 최소 점수 (CANSLIM + 컵앤핸들)
  monitor-cron: "0 */5 9-15 * * MON-FRI"  # 장중 5분마다
```

## 5. 프론트엔드 변경

### 5.1 대시보드 추가 항목
- **실시간 모니터링 상태**: 가동 중 / 중지 / 마지막 검사 시간
- **매수 신호 발생 종목 리스트**: 종목명, 현재가, 점수, 배분 금액
- **CANSLIM/컵앤핸들 분석 결과**: 종목별 상세 점수 (6요소 + 패턴)
- **최근 매매 알림**: 매수/매도 실행 내역 (실시간 갱신)

### 5.2 새로운 페이지
- `/monitor` - 실시간 모니터링 대시보드
  - 현재 모니터링 상태
  - 매수 신호 리스트
  - 종목별 분석 상세

## 6. 구현 단계

### Phase 1: 장중 모니터 워커 (핵심)
1. `IntradayMonitorWorker` 생성
2. 장중 5분마다 현재가 조회 → 분석 → 매수 실행
3. 설정 파일에 모니터 크론 추가

### Phase 2: 점수 기반 배분
1. `TradingStrategyService`에 점수 기반 배분 로직 추가
2. 매수 대상 종목의 총점 계산 및 비례 배분
3. 소수점 매수 수량 처리

### Phase 3: 프론트엔드
1. 모니터링 상태 표시 컴포넌트
2. 매수 신호 리스트 표시
3. 종목별 분석 결과 상세 보기

### Phase 4: 테스트 및 안정화
1. 시뮬레이션 모드 테스트
2. 장중 모니터링 안정성 확인
3. 로그 및 알림 개선

## 7. 고려 사항

### 7.1 API 호출 제한
- 한국투자증권 API: 초당 1회 제한
- 2,763종목 × 5분 = 약 9시간 소요 (전체 조회 시)
- **해결**: 관심 종목 필터링 또는 병렬 처리 최적화

### 7.2 동시성 제어
- 5분 간격 내 주문 중복 방지
- 포트폴리오 잔고 실시간 확인
- 주문 실패 시 재시도 로직

### 7.3 장 마감 처리
- 15:30 이후 모니터링 중지
- 장 마감 종가 동기화 (기존 3:30 PM 스케줄 유지)
- 익일 장 시작 전 데이터 준비

## 8. 성공 기준
- 장중 5분마다 현재가 조회 정상 동작
- CANSLIM/컵앤핸들 분석에 따른 매수 신호 포착
- 점수 기반 포지션 사이징 정상 계산
- 매수 주문 실행 (시뮬레이션/실전)
- 프론트엔드에서 실시간 상태 확인 가능
