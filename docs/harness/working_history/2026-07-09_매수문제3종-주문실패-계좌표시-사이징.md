# 2026-07-09 매수 활동 중 3문제 진단·해결 (주문실패·계좌표시·사이징)

## 배경
실계좌 매수 개시 후 대시보드에 드러난 3문제: ①계좌 "API 연결 실패"(총자산 0), ②주문 실패 500 `EGW00202 GW라우팅 오류`, ③"매수 수량 0 (배분 11634원)"·슬롯 초과.

## 진단 (게이트 사슬)
- **#2 = 유일한 실차단(확정).** `KoreaInvestmentApiClient`의 현금주문 tr_id가 조회용 `R`(`TTTC0802R/TTTC0801R`). 주문 엔드포인트(POST)에 R을 보내 게이트웨이가 라우트를 못 찾아 EGW00202. 잔고(TTTC8434R)는 조회라 정상 → 워커 잔고조회는 성공했고 배분값도 산출됨.
- **#1 = 관측성(비차단).** 동일 getBalance를 워커는 성공, 대시보드만 실패 → 유량제한/순단 추정. 프로덕션 SSH 차단으로 rt_cd 실측은 미확인(가설).
- **#3 = 설계.** 예수금(실제 116,000원, 이후 사용자가 ~1,000,000원으로 증액 예정)을 점수비례 균등분할 → 파편화. **소수점 매매는 KIS OpenAPI 불가(정수 ORD_QTY, 앱 전용 신탁)** — 웹 리서치로 확정. 사용자는 "예수금으로 살 수 있는 종목만" 정수 분산매수 원함.

## 변경 (커밋)
- `fa0a88b` fix: 주문 tr_id `R→U`(실전 TTTC0802U/0801U, 모의 VTTC…U), isReal 분기. 잔고 R 유지. + 대시보드 잔고조회 견고화(5초 캐시·2회 재시도·원인로깅·마지막값 폴백, 읽기경로 전용). 회귀방지 테스트 3건. **→ 배포 완료(HTTP 302).**
- `a5f8cea` chore: deploy.sh SSH 키 경로를 repo-root 상대경로로(머신 이식성). **→ 배포 완료.**
- `a47e227` feat(sizing #3): 순수함수 `planPurchases()` — 예수금필터(UNAFFORDABLE)→점수순 슬롯선정→종목당 상한10%→잔여 라운드로빈 이월. price<=0 방어. dashboard `UNAFFORDABLE='예산 초과'`. 테스트 9건. **→ 배포 대기(사용자 승인 필요).**
- 전체 141 tests green. qa-inspector 2회 PASS(차단 0).

## 리서치 (별건 산출물)
`_workspace/A_broker_fractional_research.md`: 국내주식 소수점 자동주문 가능 증권사 0곳(전 증권사 앱 전용, 신탁 배치). 토스만 "미국주식" 소수점 API 지원. **→ 국내 소수점 목적 브로커 교체 실효 없음.** 메모리 `kis-domestic-fractional-unavailable` 기록.

## 다음 단계 참조
- **미배포:** `a47e227`(사이징 #3) 프로덕션 배포는 사용자 승인 대기 중. 승인 시 `./deploy.sh`.
- **검증 필요(배포 후):** 다음 5분 사이클에서 저가 신호주 "주문 성공" 뜨는지(=#2 해소 증거), 사이징 배포 후 "예산 초과" 배지·분산 매수 확인.
- **미해결/결정 대기:**
  - 소수점이 전략상 필수면 → 미국주식 전환 + 토스 Open API PoC(지정가 지원 여부 우선 검증). 별도 대형 과제.
  - #1은 가설 — 프로덕션 로그 접근 가능 시 잔고 실패 rt_cd 실측 권장(EGW00201 유량제한 여부).
  - 기존 불일치: `TradingStrategyService` min-score 60 vs 워커 120(이번 범위 밖, 별도 트래킹).
  - 사이징 상한은 flat 10% + 점수순 우선. 실체결 관찰 후 점수가중 여부 재조정 여지.
- **관측성 공백:** 컨테이너 재배포 시 로그·메모리 신호 소실(액추에이터 없음).
