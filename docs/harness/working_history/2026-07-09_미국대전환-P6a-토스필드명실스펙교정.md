# 미국 대전환 P6a — 토스 캔들/토큰 필드명 실스펙 교정

날짜: 2026-07-09 · 브랜치: us-pivot · 리스크: 표준(실스펙 정합, 로직 불변) · 판정: clean test 191 그린

## 배경
P6 착수 전, 토스 실제 openapi.json/llms.txt를 실측했다. P5가 B실사 "가정값"으로 구현한 캔들/토큰 필드명이 실제와 달라(캔들 조용히 0건 리스크) 먼저 교정. 동시에 **주문 프리미스 재확인**: llms.txt가 "Order: create, modify, cancel, list, detail, buying power, sellable quantity, commissions" 카테고리를 명시 → `POST /api/v1/orders`·modify·cancel·`GET /api/v1/order-info` 실재 확인(첫 openapi.json 페치 요약모델이 놓쳤던 것). 전환 프리미스 유효.

## 변경 요약 (필드명/매핑/픽스처만, 로직 불변)
- 토큰: `POST /oauth2/token`, application/x-www-form-urlencoded, `client_id`/`client_secret`(P5 appKey/appSecret·JSON·`/api/v1/oauth/token`에서 교정). TossProperties·TossTokenProvider.
- 캔들: 쿼리 `symbol`(P5 code), 응답 `timestamp`/`openPrice`/`highPrice`/`lowPrice`/`closePrice`/`volume`(P5 date/open/…). TossCandleClient. parse는 envelope 배열직접/래핑 양쪽, mapCandle은 timestamp ISO8601/epoch 양쪽 견고 처리.
- 픽스처 candles-page1/2.json 실스펙 형태로 교체. 테스트 2곳(symbol 쿼리, 토큰 URL) 단언 교정.

## 검증
`./gradlew clean test` 191 tests, 0 fail/err. 어댑터 골격·포트·영속화·분석기·전략 변경 0.

## 다음 단계 참조
- **P6 본체(모의투자)**: TossBrokerAdapter(BrokerPort) — `POST /api/v1/orders`(생성)·modify·cancel·`GET /api/v1/order-info`(매수가능금액·매도가능수량·수수료)·`GET /api/v1/accounts`(accountSeq)·`GET /api/v1/assets`(보유·USD현금)·`GET /api/v1/exchange-rate`. + 소수점 금액기반(orderAmount) 주문 + 사이징 notional화(planPurchases 출력 정수수량→금액, IntradayMonitorWorkerPlanTest 갱신 수반=행위변경) + MarketCalendarPort(토스 US calendar). **중대 등급 — 커밋 사용자 승인 필수.**
  - **미확정(샌드박스 실호출로 확정)**: 주문 바디 필드(orderAmount/quantity/side/orderType 정확명), 잔고 USD현금 필드명, 캔들 envelope/timestamp 타입. 실 모의투자엔 토스 sandbox app-key/secret 필요(사용자 제공).
- **누적 배포 체크리스트**: eps/debtRatio 수동 ALTER(P3), edgar.user-agent·toss.* 설정.
- **핵심 결정 유지**: 유니버스 미국 국내보통주, 데이터비용 $0.
