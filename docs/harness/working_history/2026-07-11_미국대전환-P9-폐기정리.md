# 미국 대전환 P9 — 폐기 정리 (데드코드·레거시 엔드포인트·설정키·벤더명)

- **날짜**: 2026-07-11
- **브랜치**: `us-pivot`
- **리스크 등급**: 표준(데이터수집/정리·표시·설정). 주문·사이징·점수 로직 불변. 삭제는 하드-투-리버스라 보존대상 무결 검증.
- **범위**: 설계 §7 P9(KRX/DART 클라이언트·DTO·corp_code_map·Kakao 제거) + 레거시 동기화 엔드포인트 **전부 제거**(사용자 결정) + 벤더명 필드 rename(P8 QA W2).
- **게이트 결과**: qa-inspector 7개 경계면 **PASS(차단 0)**, `./gradlew test` **208 tests 0 fail**(224→208, 삭제 테스트만큼 감소, backend·qa·리더 재현).

## 무엇을 했나

### 삭제 (참조 해소 후 — 데드 확정된 것만)
- **클래스**: `KrxApiClient`, `KrxDataSyncService`, `DartApiClient`, `DartDataSyncService`, `KakaoNotificationService`.
- **DTO**: `KrxCorpDTO`, `KrxApiResponse`, `KrxPriceDTO`, `DartApiResponse`, `DartCompanyDTO`, `DartFinancialDTO`.
- **리소스**: `corp_code_map.json`.
- **테스트**: `KrxDataSyncServiceTest`, `DartDataSyncServiceTest`, `KakaoNotificationServiceTest`.

### 참조 해소 (삭제 선행)
- **StockController**: 레거시 엔드포인트 3개 제거 — `POST /stocks/sync`(KRX 종목동기), `/stocks/sync/sectors`(DART 섹터), `/stocks/preset`(한국 프리셋). 유지: 목록·상세·수동등록·활성/비활성·검색·counts·`/sync/bulk-prices`·`/sync/bulk-financials`(토스/EDGAR 경유).
- **StockService**: 레거시 메서드 4개 제거(syncStockListFromKrx·syncSectorsFromDart·registerStockFromKrx·registerPresetStocks) + KrxApiClient/DartApiClient 주입 제거. 벤더무관 메서드 유지.
- **StockServiceTest**: 제거 메서드·KRX/DART 목 테스트만 제거, 나머지 유지.
- **WiringTest 갱신**: `MarketDataPortWiringTest`·`FinancialsPortWiringTest`에서 삭제 클래스 참조 라인만 제거, **핵심 단언(TossMarketDataAdapter 유일 MarketDataPort·EdgarFinancialsAdapter 유일 FinancialsPort) 유지**(qa 확인 — 껍데기 약화 없음).
- StockPriceUpserter/SecTickerUniverseLoader/DataSyncScheduler 잔존 주석 정합.

### 설정키 정리 (application.yml + application-prod.yml)
- **제거**: `api.dart.*`, `api.krx.*`, `kakao.*`.
- **유지**: `api.korea-investment.*`, `toss.*`, `edgar.*`, `notification.telegram.*`, `trading.*`, datasource/jpa.

### 벤더명 필드 rename (P8 QA W2)
- 실주입 실체와 필드명 정합: `koreaInvestmentApiClient`(실주입=@Primary TossBrokerAdapter) → **`brokerPort`**, `dartDataSyncService`(실주입=EdgarFinancialsAdapter) → **`financialsPort`**.
- 4개 파일: DashboardController·TradingStrategyService·IntradayMonitorWorker·PortfolioScheduler. 타입 불변, 순수 rename. 구 필드명 잔재 0.

### 대시보드
- `stock-list.html`: 레거시 버튼 제거(프리셋 등록·KRX 동기화; DART 섹터는 원래 버튼 없었음). "KRX/DART/프리셋" 문구 벤더중립·제거. bulk-prices/bulk-financials·수동등록·활성/비활성 유지. action-bar flex라 레이아웃 무손상.
- `dashboard.html`: "KRX 시세" 툴팁 → "시세"(엔드포인트 유지, 데이터는 토스).

## ★보존 대상 무결 (qa 확인)
- `KoreaInvestmentApiClient`(broker **폴백** @Service, implements BrokerPort) 존재·컴파일·KIS 내부 DTO 유지. `api.korea-investment.*` 양 yml 유지.
- broker 진리표(toss on→Toss·off→KIS) 여전히 성립(BrokerPortWiringTest 통과). 토스/EDGAR/텔레그램 활성경로 불변.
- **설계 §6 이탈 명시**: §6는 `api.korea-investment.*` 폐기로 계획했으나, P8에서 KIS를 broker 폴백으로 유지 결정 → 해당 설정키·빈·DTO는 **의도적 보존**(설계 대비 이탈, 근거 기록).

## 검증 (qa-inspector, 차단 0)
orphan 참조 0(주석만)·스프링 컨텍스트 기동·보존대상 무결·포트 유일빈 단언 유지·레거시 엔드포인트/템플릿 POST 0·rename 정합·설정키 제거 — 7개 전부 PASS. `./gradlew test` 208 통과.

## 다음 단계 참조 — 미국 대전환 P1~P9 완료
- **★커밋 대기**: QA PASS 상태 **미커밋**. 사용자 승인 후 단일 커밋. 배포·push 별도.
- **us-pivot 전체 완료 상태**: 포트 3+캘린더 뒤로 토스(시세·주문)·SEC EDGAR(재무) 어댑터, 가격 BigDecimal, notional 사이징, ET 스케줄, USD 표시, broker=토스(us-pivot 기본), 데드코드 정리까지. CANSLIM 엔진·게이트·관측성·알림은 그대로 미국에서 동작.
- **실배포 게이트(4개 모두 충족 전 금지)**: ①토스 실승인·키 ②PoC로 가정상수 확정(assets USD현금 필드명·계좌헤더·소수주문 실필드·환전방식·rate limit·서머타임 — 각 어댑터 `=== PoC 미확정 ===` 표식) ③`application-prod.yml` `toss.broker.enabled=true` + KIS 폴백 해제 판단 ④배포 승인. 현재 prod는 broker 꺼둠(KIS 폴백) — 안전.
- **남은 선택 정리(비필수)**: `config` 중첩 프로퍼티 클래스에 KRX/DART 잔재 있으면 정리(backend9 스코프경계로 미삭제 명시), DashboardE2ETest 본문 어서션(`$` 존재·`원` 부재) 추가(P8 W1), 유니버스 수동 로드 UI 필요 시 SecTickerUniverseLoader 엔드포인트 신설(P9에서 "전부 제거" 결정 — 현재 나이틀리/부트스트랩만).
- **핵심 결정 유지**: KIS 폴백 보존(설계 §6 이탈), broker=토스(prod 게이트), 데이터비용 $0.
