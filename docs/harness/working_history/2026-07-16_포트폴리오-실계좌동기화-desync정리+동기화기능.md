# 포트폴리오 실계좌 동기화 — phantom desync 정리 + 브로커기준 동기화 기능

- **날짜**: 2026-07-16
- **리스크**: 중대(매매 게이트에 쓰이는 portfolio 상태 변경)
- **발단**: 대시보드는 보유 10종목·"최대 보유 종목 수 도달"로 신규 매수 전부 차단인데, 실 토스 계좌는 **0종목·현금 $77.53**. AXP(120)·C(127) 매수 신호가 phantom 보유에 막힘.

## 근본원인
`TradingStrategyService.executeBuy`(205-217행)가 `realTrading` 여부와 무관하게 `Portfolio`+`Trade`를 저장. 07/13 **관측모드(realTrading=false)**에서 신호 10건이 실주문 없이 DB에만 active=true로 적재(phantom). `maxPositions=10`(`countByActiveTrue`) 도달 → 실매수 차단.

## ① 즉시 데이터 정리 (완료)
- 실계좌 라이브 확인: 토스 `/holdings` = 0종목, `/buying-power` = $77.53. DB active portfolio 10건(id 10~19, 전부 07/13) 확인.
- 백업: 서버 `~/canagent_db_backup_presync_20260716_044609.sql.gz`(757K, 전체 pg_dump).
- 트랜잭션 삭제: `DELETE FROM trades WHERE trade_date_time::date='2026-07-13'`(10) + `DELETE FROM portfolios WHERE created_at::date='2026-07-13'`(10). 결과 active 0 / 포트폴리오 0 / 트레이드 0 → 실계좌와 일치.

## ② 재발 방지 — 브로커기준 동기화 기능 (구현·QA PASS, 미배포)
| 파일 | 변경 |
|------|------|
| `domain/portfolio/Portfolio.java` | `deactivate()`, `reconcileTo(quantity, avgBuyPrice)` 추가(하드삭제 금지) |
| `service/PortfolioService.java` | `BrokerPort`·`StockRepository` 주입, `ReconcileSummary`, `@Transactional reconcileFromBroker()` |
| `web/DashboardController.java` | `@PostMapping("/sync/portfolio")` (try/catch, flash `tradeResult`, redirect:/) |
| `templates/dashboard.html` | "수동 작업" 섹션에 "실계좌 동기화" 버튼(POST, confirm) |
| `test/.../PortfolioReconcileServiceTest.java` | 신규 6케이스 |

**reconcile 로직(안전순):**
1. **최우선 가드** — `getBalance().success==false`(토큰/API 실패)면 DB 미접근·변경 0 즉시 반환(브로커 장애 시 전종목 청산 대참사 방지). `never()`로 실증.
2. success면 broker holdings(symbol=Stock.code)로: 미보유 active→`deactivate`, 수량/평단 상이→`reconcileTo`(덮어쓰기), 신규·Stock존재→add/미존재→skip+WARN, 일치→무변경.

**검증:** QA(qa-inspector) PASS, 차단 0. success 가드·심볼매칭(GOOG/GOOGL distinct) 명확 판정. `./gradlew test` **273 / 0 실패**. 스키마 무변경.

## 다음 단계 참조
- **미배포·미커밋**(작성 시점). 커밋·배포는 사용자 승인 후. main push=자동배포(~재시작, REAL_TRADING=true는 서버 .env 유지).
- 배포 후 검증: 대시보드 "실계좌 동기화" 클릭 → 현재 실보유 0·DB 0이라 "브로커 보유 0, 청산 0..." no-op flash 확인(엔드포인트 라이브 왕복 검증).
- **알려진 잔여(선택)**: `executeBuy`/`executeSell`이 sim 모드에서도 Portfolio 저장 → realTrading=true인 현재는 재발 안 하나, 방어적으로 sim에서 실 portfolio 미기록하도록 가드 추가 여지. reconcile 버튼이 상시 교정 수단이라 우선순위 낮음.
- 실계좌 desync 재발 시 이 버튼으로 즉시 교정 가능.
