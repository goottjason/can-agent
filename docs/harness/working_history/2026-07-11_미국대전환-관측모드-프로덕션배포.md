# 미국 대전환 — 관측모드 프로덕션 배포 (#9 phase A~C)

- **날짜**: 2026-07-11
- **브랜치/커밋**: `us-pivot`(=main) `d3500e0` 배포
- **리스크**: 중대(실서버 배포). 사용자 승인 하 진행. **관측모드**(REAL_TRADING=false → 실주문 0).
- **서버**: 168.107.31.154 (OCI ARM aarch64), `projects-can-agent-1` 컨테이너.
- **전제**: 사용자가 기존 KIS/국내 배포 완전 폐기 확정, 자유 덮어쓰기 승인. 활성 국내 포지션 0.

## Phase A — DB
- pg_dump 백업(15M, `~/canagent_db_backup_*.sql.gz`).
- 마이그레이션 적용·검증: `eps` numeric(19,2→4), `debt_ratio` (5→12,2), `fiscal_quarter` NOT NULL 해제. (최초 heredoc `docker exec`에 `-i` 누락으로 미적용 → `-c`로 재적용·검증하여 확정.)
- 폐기 백업테이블 `financial_statements_bak_20260708` 드롭.
- 국내 종목 2763건(KOSPI/KOSDAQ/KONEX) `active=false` 비활성.

## Phase B — 배포
- 서버 working tree 화해: 미커밋 변경(Dockerfile/.dockerignore/.gitignore/build.gradle) diff 백업 후 폐기, 미추적 파일(WebConfig/DotenvConfig/TradeAuthInterceptor 등, 우리 repo에 추적본 존재) `git stash -u` 백업 → `git pull origin main`(→d3500e0).
- 관측모드 env: 서버 `.env` `TOSS_BROKER_ENABLED=true`(토스 broker 선택 — USD 잔고·현재가), `REAL_TRADING=false`(주문 차단). compose environment에 REAL_TRADING 전달 추가.
- 멀티스테이지 소스 빌드 + 기동: `docker compose up -d --build can-agent`. prod 프로파일, 6.8초 기동, 헬스체크 HTTP 302. 다른 서비스(sbshop/nginx/postgres) 무영향.
- 컨테이너 env 확인: TOSS_BROKER_ENABLED=true·REAL_TRADING=false·TOSS_ACCOUNT=1·SPRING_PROFILES=prod·EDGAR_USER_AGENT 실연락처.

## Phase C — 데이터 적재
- 유니버스: `POST /stocks/load-universe` → 100종목 upsert(NYSE 64·NASDAQ 36), totalActive=100.
- 재무 백필(EDGAR 8분기): 99종목/921건 upsert(1종목 us-gaap facts 없음 — 무해 경고).
- 시세 백필(토스 캔들 300일): 100종목/33000건(2025-03-18~2026-07-11 ~16개월).
- **★P6b 캔들 result-언랩 수정 프로덕션 검증**: 토스 캔들이 실제 저장됨(과거 "조용한 0건" 버그 해소 확인).
- 심각 에러 없음, 컨테이너 안정.

## 관측 결과
- US 100종목 · 시세 100 · 재무 99 · 실주문 0(REAL_TRADING=false).
- 점수(analysis_scores)는 스케줄(AutoTradingWorker 07:00 ET MON-FRI)로 산출 — 수동 트리거 없음. 월요일 개장 전 자동 실행.

## 다음 단계
- **월요일(2026-07-13)**:
  1. 07:00 ET 점수 스케줄 로그 확인(CANSLIM 산출 정상 여부).
  2. 정규장(KST 22:30~) **실주문 PoC(#7)**: `scripts/toss-poc-order.sh` Phase1(무체결)→Phase2($2 실체결). 소수주문 최소금액·정규장·execution 확정.
- **PoC 통과 후(별도 승인)**: `.env` `REAL_TRADING=true` → 재시작 → 실 US 자동매매 활성.
- **#8 CI/CD**: GitHub secret `CANAGENT_DEPLOY_KEY` 등록(사용자) → deploy.yml push 트리거 해제.
- **롤백**: 문제 시 `~/canagent_db_backup_*` 복원 + 이전 이미지/커밋. 컨테이너만 재기동은 `docker compose restart can-agent`.
- **백업 위치**: DB `~/canagent_db_backup_20260711_*.sql.gz`, 서버 로컬변경 `~/canagent_server_localdiff_*.patch` + git stash.
