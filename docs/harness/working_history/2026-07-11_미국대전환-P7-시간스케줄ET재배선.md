# 미국 대전환 P7 — 시간·스케줄 ET 재배선

- **날짜**: 2026-07-11
- **브랜치**: `us-pivot`
- **리스크 등급**: 표준(중대 경계) — 스케줄 존/시각이 실거래 발동 타이밍 지배. 커밋 사용자 승인 후.
- **범위**: 설계 §5(시간·스케줄 ET 재작성). P6에서 선적재된 `MarketCalendarPort`/`TossMarketCalendarAdapter`를 실배선.
- **게이트 결과**: qa-inspector 경계면 교차검증 **PASS(차단 0)**, `./gradlew test` **222 tests 0 fail**(backend·qa·리더 통합 3회 재현).

## 무엇을 했나

### 1. 존 전환 `Asia/Seoul` → `America/New_York` (DST 자동)
- `@Scheduled(zone)` **6개 전부** ET: IntradayMonitorWorker·AutoTradingWorker·DataSyncScheduler(2)·PortfolioScheduler(2).
- 코드 내 `ZoneId.of("Asia/Seoul")`/`now(KST)` 전수 ET. `grep Asia/Seoul src/main` = 실행경로 0건(MarketCalendarPort.java 주석 1건만 의도 보존).

### 2. isTradingHours 3중복 → 공유 유틸 `service/MarketHours`
- `IntradayMonitorWorker`·`DashboardController`의 private isTradingHours(9:00~15:30 KST) **제거** → `MarketHours` 단일 소비(단일 진실).
- `MarketHours(MarketCalendarPort, Clock)`: `isTradingHours()`·`isTradingHours(Instant)`·`isTradingDay(LocalDate)`·`todayEt()`·`nowEt()`·`now()`. `MarketCalendarPort`의 유일 소비자.
- `Clock` 빈(`SchedulingConfig.systemClock()`=UTC) 주입 → 테스트는 `Clock.fixed()`로 특정 순간 검증.

### 3. 정적 NYSE 휴일 폴백 (TossMarketCalendarAdapter)
- 주말만 판정 → **미국 거래소 휴일 포함**(독립기념일 등 오주문 방지). 규칙계산(연도무관):
  - 고정일+관측대체(토→직전금·일→직후월): 신정·준틴스(6/19)·독립기념일(7/4)·성탄절.
  - 요일기반: MLK·워싱턴탄신일·메모리얼·노동절·추수감사절.
  - 계산: 성금요일(부활절−2, Anonymous Gregorian).
- 토스 US market-calendar 실연동은 `=== PoC 미확정 ===` 표식(실호출 확정 시 교차검증/교체).

### 4. 크론 ET 교정 (코드 @Scheduled + application.yml + application-prod.yml 3원 일치)
| 키 | 전(KST) | 후(ET) | 근거 |
|---|---|---|---|
| 야간점수 `cron` | 0 0 19 @Seoul | `0 0 7 * * MON-FRI` @NY | EDGAR 나이틀리·재무동기화 이후·개장 전 |
| 주가·재무 `sync-cron` | 0 30 15 @Seoul | `0 0 4 * * MON-FRI` @NY | SEC 03:00 ET ZIP 재컴파일 이후 |
| `quarterly-cron` | 0 0 10 1 1,4,7,10 @Seoul | 동값 @NY | 존만 |
| `monitor-cron` | 0 */5 9-15 @Seoul | 동값 @NY | 존만(9:00~9:29 틱은 개장게이트 필터) |
| `price-cron` | 0 */5 9-15 @Seoul | 동값 @NY | 존만 |
| `close-price-cron` | 0 0 16 @Seoul | 동값 @NY | 정규장 마감 16:00 ET |
- yml의 KST 주석("오후 3시 30분" 등)도 ET로 교정.
- **yml 사전결함 교정**: `application.yml` quarterly-cron 6필드 day/month 뒤바뀜, `application-prod.yml` 7필드(Spring 6필드 크론 파싱오류 위험) → 둘 다 `0 0 10 1 1,4,7,10 *`로 정렬. 매매 임계값 불변.

### 5. 대시보드 템플릿 KST→ET 표시 교정 (dashboard-builder)
- `system-overview.html`(L53·59·65·96·99·100)·`system-monitoring.html`(L34·42·58·66·86·91·96·101)·`system-trading.html`(L68): 15:30 KST→04:00 ET, 19:00 KST→07:00 ET, 09:00~15:30 KST→09:30~16:00 ET, zone/존 설명 ET. **표시 텍스트만**(바인딩·JS·CSS 불변). `grep KST templates/` = 0건.
- `dashboard.html`·`monitor.js`는 동적 serverTime(이미 ET) 소비 — 불변.

## 검증 (qa-inspector, 차단 0)
존 일관성·크론 3원 일치·공유 유틸 단일진실·휴일 폴백 날짜 독립계산·개장경계(9:30/16:00 포함·양끝 차단)·DST 전후(2026-03-09·11-02) 전부 PASS. `MarketHoursTest`·`TossMarketCalendarAdapterTest` 어서션 유의미(Clock.fixed 시각고정). `./gradlew test` 222 통과(통합 재현).

## 다음 단계 참조
- **★커밋 대기**: QA PASS 상태 **미커밋**. 사용자 승인 후 단일 커밋. 배포·push 별도 승인.
- **P8(표시층·정리) 이관 항목**:
  - **벤더/서사 텍스트 잔존**(P7 범위 밖으로 보류): `system-monitoring.html` L35·`system-overview.html` L54 프로즈에 "**한국투자증권 API**"(→토스)·"**DART**"(→SEC/EDGAR) + "**장 마감 후**"(04:00 ET는 실제 개장 전=pre-market이라 서사 모순) 잔존. 대전환 벤더 정본 용어로 교정 필요.
  - `monitor.js` `toLocaleTimeString('ko-KR')` 로케일 표시(표시층).
  - `DashboardController` 필드명 `krxDataSyncService`(타입 MarketDataPort) rename.
- **PoC/실측 후 미세조정**: 야간점수 07:00 ET·재무 04:00 ET는 §5 근거의 합리적 선택값 — 실제 EDGAR 배치 완료시각 실측 후 조정 여지. 토스 US market-calendar 실연동 시 정적 휴일집합 교차검증.
- **핵심 결정 유지**: KIS `@Primary`(국내 경로 유지), 토스 프로퍼티 활성, 데이터비용 $0. P7은 존/시각만 변경 — 주문·사이징·min-score 로직 불변(P6 완료분).
